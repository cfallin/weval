- SM+weval
  - (X) constant memory for resumeOffsets and tableSwitchCaseOffset
    - transitively-constant memory?
  - (X) finish switch-meet-with-constant update
  - update all SwitchValue and SwitchDefault values to scalar Constants when
    meeting blockparams
    - need index in SwitchDefault too