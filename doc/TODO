SSA revamp:

- define context in terms of domtree
  - invariant: a value used at a block in a given context can only be
    defined in that context or a parent context
  - static pass to find blocks that set PC; in context, note block
    where set, and pop when going to block not dom'd by it